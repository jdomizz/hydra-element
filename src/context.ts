/**
 * The `hydra-element/context` subpath entry — evaluation contexts + primitives.
 */

export { createContext, hydraEval, userCodeLine } from './core/eval'
export type { Context } from './core/eval'
